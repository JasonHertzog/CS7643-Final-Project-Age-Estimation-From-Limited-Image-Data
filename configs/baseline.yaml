experiment_name: "baseline_scratch"
model_name: "base"
seed: 42

# Data parameters
batch_size: 32
num_workers: 4
image_size: 224

# Training parameters
epochs: 5
learning_rate: 0.001
optimizer: "Adam"
loss_function: "MSELoss"
save_best: True
